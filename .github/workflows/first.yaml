name: First Workflow
on: [push]
jobs:
  run-shell-commands: 
    runs-on: ubuntu:latest
    steps:
      - name: echo a string
        run: echo "hello"
      - name: Multiline Coammnd
        run: |
          node -v 
          npm -v 

